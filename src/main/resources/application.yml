server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:forli_despacho}
    username: ${DB_USER:forli}
    password: ${DB_PASS:forli2026}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQLDialect
      hibernate.format_sql: true

app:
  cors:
    allowed-origins: ${CORS_ORIGINS:http://localhost:5173}
